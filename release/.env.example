# Variables de entorno de ejemplo para GóndolaPro

# Puerto interno del backend
PORT=3000

# Configuración de SQL Server dentro de Docker
DB_HOST=db
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=TuPasswordSegura123
DB_DATABASE=ProyectoBD

# Configuración general
NODE_ENV=production

# JWT / autenticación
JWT_SECRET=cambiar_este_valor_en_produccion